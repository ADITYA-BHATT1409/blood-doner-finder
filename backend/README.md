# Backend

This folder is reserved for the backend implementation (e.g. Node.js, Spring Boot, Python).
Currently, the frontend is built entirely using HTML/CSS/JS with mock data in the `frontend` directory.
