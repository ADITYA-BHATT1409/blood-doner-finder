{
  "name": "backend",
  "version": "1.0.0",
  "description": "This folder is reserved for the backend implementation (e.g. Node.js, Spring Boot, Python). Currently, the frontend is built entirely using HTML/CSS/JS with mock data in the `frontend` directory.",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
